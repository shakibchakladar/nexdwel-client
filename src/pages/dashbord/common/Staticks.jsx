// import useRole from '../../../components /hooks/useRoll'

// const Statistics = () => {
//   const [role, isLoading] = useRole()
//   if (isLoading) return <h2>Loading...</h2>
//   return (
//     <>
//       {role === 'admin' && <AdminStatistics />}
//       {role === 'agent' && <HostStatistics />}
//       {role === 'user' && <GuestStatistics />}
//     </>
//   )
// }

// export default Statistics



const Staticks = () => {
    return (
        <div>
            <h2>Welcome to statics page ...</h2>
            
        </div>
    );
};

export default Staticks;